{
    "name": "firebase",
    "version": "0.1.0",
    "private": true,
    "dependencies": {},
    "devDependencies": {
        "@types/node": "^17.0.8",
        "firebase-tools": "^10.0.1",
        "npm-run-all": "^4.1.5",
        "typescript": "^4.5.4"
    },
    "scripts": {
        "dev": "firebase serve -p 4000",
        "deploy": "firebase deploy",
        "deploy-functions": "firebase deploy --only functions"
    },
    "proxy": "http://localhost:4000"
}
